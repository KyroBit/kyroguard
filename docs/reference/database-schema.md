# Database schema

`@kyrobit/rbac` stores everything in six tables.

| Table | Stores |
| --- | --- |
| [`rbac_policies`](#rbac_policies) | One row per synced policy. |
| [`rbac_policy_groups`](#rbac_policy_groups) | One row per group (role). |
| [`rbac_policy_group_policies`](#rbac_policy_group_policies) | Which policies each group grants. |
| [`rbac_user_policy_groups`](#rbac_user_policy_groups) | Which user has which group. |
| [`rbac_user_policies`](#rbac_user_policies) | Direct policy grants to users. |
| [`rbac_resource_owners`](#rbac_resource_owners) | Who owns which of your app's rows. No foreign keys into your tables. |

The Drizzle schemas ship at `@kyrobit/rbac/drizzle/schema/{pg,mysql,sqlite}`. The Prisma models ship as `prismaSchemaSnippet` and are scaffolded to `prisma/rbac.prisma`. Mongoose creates matching collections and indexes when `rbac sync` runs.

Types below are PostgreSQL. The MySQL, SQLite and MongoDB equivalents ship in the packaged schemas, with the same names and constraints. Two notes if you ever write rows by hand:

- `portal`, `context_id` and `context_type` are never null. Store `''` when there is no value.
- On MySQL, key columns are `varchar(191)`. Ids, names and context values must fit 191 characters.

## rbac_policies

One row per synced policy. `name` includes the portal prefix, for example `admin.posts.read`.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | `text` | Primary key. cuid2, generated by the app. |
| `name` | `text` | Full policy name. |
| `portal` | `text` | Portal it was synced under. Default `''`. |
| `label` | `text` | Human-readable label. |
| `scope_options` | `jsonb` | Allowed scope names. Default `[]`. |
| `depends_on` | `jsonb` | Names of required policies. Default `[]`. |
| `created_at` | `timestamp` | Default `now()`. |
| `updated_at` | `timestamp` | Default `now()`. |

| Constraint | Purpose |
| --- | --- |
| `name` UNIQUE | One row per policy name. |

## rbac_policy_groups

One row per group. Groups are roles.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | `text` | Primary key. cuid2, generated by the app. |
| `name` | `text` | Group name, for example `editor`. |
| `label` | `text` | Human-readable label. |
| `description` | `text` | Optional. |
| `is_system` | `boolean` | Marks built-in groups. Default `false`. |
| `is_active` | `boolean` | Default `true`. |
| `created_at` | `timestamp` | Default `now()`. |
| `updated_at` | `timestamp` | Default `now()`. |

| Constraint | Purpose |
| --- | --- |
| `name` UNIQUE | One row per group name. |

Set `is_active` to `false` to switch a group off everywhere. Direct grants keep working.

## rbac_policy_group_policies

Which policies a group grants, and with what scope.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | `text` | Primary key. cuid2, generated by the app. |
| `policy_group_id` | `text` | References `rbac_policy_groups.id`. |
| `policy_id` | `text` | References `rbac_policies.id`. |
| `scope` | `text` | Scope name, or null for unrestricted. |
| `created_at` | `timestamp` | Default `now()`. |

| Constraint | Purpose |
| --- | --- |
| `rbac_pgp_group_policy_uq` UNIQUE (`policy_group_id`, `policy_id`) | Group grants each policy once. |

## rbac_user_policy_groups

Group assignments: which user has which group, in which portal and context.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | `text` | Primary key. cuid2, generated by the app. |
| `subject_id` | `text` | Your user's id. No foreign key, users live in your tables. |
| `policy_group_id` | `text` | References `rbac_policy_groups.id`. |
| `portal` | `text` | Default `''`. |
| `context_id` | `text` | Default `''`. |
| `created_at` | `timestamp` | Default `now()`. |

| Constraint | Purpose |
| --- | --- |
| `rbac_upg_tuple_uq` UNIQUE (`subject_id`, `policy_group_id`, `portal`, `context_id`) | Stops duplicate assignments. |
| `rbac_upg_subject_idx` (`subject_id`) | Fast grant lookups. |

## rbac_user_policies

Direct policy grants, with an optional scope.

| Column | Type | Notes |
| --- | --- | --- |
| `id` | `text` | Primary key. cuid2, generated by the app. |
| `subject_id` | `text` | Your user's id. No foreign key. |
| `policy_id` | `text` | References `rbac_policies.id`. |
| `portal` | `text` | Default `''`. |
| `context_id` | `text` | Default `''`. |
| `scope` | `text` | Scope name, or null for unrestricted. |
| `created_at` | `timestamp` | Default `now()`. |

| Constraint | Purpose |
| --- | --- |
| `rbac_up_tuple_uq` UNIQUE (`subject_id`, `policy_id`, `portal`, `context_id`) | Stops duplicate grants. |
| `rbac_up_subject_idx` (`subject_id`) | Fast grant lookups. |

## rbac_resource_owners

Who owns which of your app's rows. This backs `Scope.owned()`. See [Ownership](/guide/ownership).

| Column | Type | Notes |
| --- | --- | --- |
| `id` | `text` | Primary key. cuid2, generated by the app. |
| `resource_type` | `text` | Your resource type, for example `post`. |
| `resource_id` | `text` | Your row's id. |
| `owner_id` | `text` | The owning user's id. |
| `context_type` | `text` | Portal at creation time. Informational. Default `''`. |
| `context_id` | `text` | Context at creation time. Informational. Default `''`. |
| `created_at` | `timestamp` | Default `now()`. |

| Constraint | Purpose |
| --- | --- |
| `rbac_ro_tuple_uq` UNIQUE (`resource_type`, `resource_id`, `owner_id`) | Records each owner once. |
| `rbac_ro_resource_idx` (`resource_type`, `resource_id`) | Fast ownership checks. |

## Ids and timestamps

- Primary keys are cuid2 strings. The app generates them, not the database.
- MongoDB uses standard ObjectIds instead.
- On SQLite, timestamps have no database default. Rows inserted outside the library must set them.
- Deleting a policy also deletes its group entries and direct grants, in one transaction.

## Next steps

- [Drizzle](/databases/drizzle) to add the schema to your migrations.
- [Prisma](/databases/prisma) to include the models in your Prisma schema.
- [MongoDB](/databases/mongodb) for collections and indexes.
